numpy==2.3.5
matplotlib==3.10.8
scipy==1.16.3
# the MEDCAP mrd fork, imported as `mrd`, pinned to the dev branch commit the mrd-fork submodule
# records (mrd2recon.py writes NdArrays, which exist only on dev). Keep the two in step: after
# `git submodule update --remote mrd-fork`, copy `git -C mrd-fork rev-parse HEAD` here.
mrd-python @ git+https://github.com/MEDCAP/mrd-fork.git@9fe591ddf4a6ee5d53a066dbfda5124e965760d1#subdirectory=python
