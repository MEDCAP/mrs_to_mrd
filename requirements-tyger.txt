# Runtime dependencies for the Tyger conversion containers. Deliberately smaller than
# requirements.txt: conversion needs neither matplotlib nor scipy, which are only used by the
# recon, fitting, and plotting scripts.
#
# The mrd fork is pinned to a commit rather than a branch so builds are reproducible. This revision
# carries the three fork-specific fields generate_acquisition writes (acquisition_time_stamp_ns,
# sample_time_ns, Acquisition.phase). It does not carry the NdArray types, which conversion does not
# use but mrd2recon.py does, so do not reuse this pin for a recon image.
#
# Note the fork's setup.py reads the version from $MRD_VERSION_STRING, so that variable must be set
# in the environment for pip install to succeed. The Dockerfile sets it.
numpy==2.3.5
mrd-python @ git+https://github.com/MEDCAP/mrd-fork.git@9a63b7ff2f18eb468db4d9a4dc4eef0f0af2bd21#subdirectory=python
